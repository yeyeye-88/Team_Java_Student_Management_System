# 学生管理系统实验报告

## 1. 项目描述

### 1.1 项目背景
学生管理系统是教育信息化的重要组成部分，旨在通过信息技术手段对学生的学习生活进行全面、高效、科学的管理。本项目为山东大学Java课程设计，实现了学生管理的核心功能，包括学生信息管理、教学管理、考勤管理、请假审批、奖惩管理、创新活动管理等。

### 1.2 项目目标
- 构建一个功能完善的学生管理系统，提高学校管理效率
- 实现学生信息的数字化、规范化管理
- 优化请假审批流程，提升办公自动化水平
- 提供多维度的数据统计分析，辅助决策制定
- 保障系统安全性，确保数据完整性

### 1.3 项目范围
- 用户权限管理（管理员、学生、教师三种角色）
- 学生基本信息管理
- 教学管理（课程、课表、成绩）
- 考勤管理
- 请假审批流程
- 奖惩管理
- 创新活动管理
- 数据统计分析

## 2. 功能需求

### 2.1 用户权限管理
- **用户注册/登录**：支持用户注册和登录功能，使用JWT进行身份验证
- **角色管理**：系统支持管理员、学生、教师三种角色
- **权限控制**：不同角色拥有不同的功能访问权限
- **密码管理**：支持用户修改密码功能
- **菜单管理**：根据用户角色动态显示菜单项

### 2.2 学生信息管理
- **学生信息录入**：录入学生基本信息（学号、姓名、性别、生日、身份证号、邮箱、电话、地址、专业、班级等）
- **学生信息查询**：支持按学号、姓名等条件查询学生信息
- **学生信息修改**：允许授权用户修改学生信息
- **学生信息删除**：支持删除学生信息（需遵循数据完整性约束）
- **家庭成员管理**：管理学生家庭成员信息（姓名、关系、电话）
- **学生照片上传**：支持学生照片上传和存储

### 2.3 教学管理
- **课程管理**：课程信息的增删改查（课程名称、学分、授课教师）
- **课表管理**：安排课程时间地点，支持按星期、节数排课
- **成绩管理**：学生各科成绩的录入、查询和统计分析
- **成绩统计**：按课程、班级等维度统计平均分、最高分、最低分等

### 2.4 考勤管理
- **考勤记录**：记录学生上课出勤情况（出勤、缺勤、迟到、请假）
- **考勤统计**：按课程、学生进行考勤率统计分析
- **考勤查询**：支持按学生、课程、时间段查询考勤记录

### 2.5 请假审批流程
- **请假申请**：学生可提交请假申请（病假/事假），填写请假时间、原因、时长
- **审批流程**：教师或管理员审批，支持通过/驳回及审批意见
- **状态跟踪**：实时跟踪请假申请状态（待审批、已通过、已驳回）
- **请假统计**：统计请假频率、通过率等信息

### 2.6 奖惩管理
- **荣誉记录**：记录学生获得的荣誉和奖励
- **处分记录**：记录学生的违纪处分情况
- **奖惩统计**：统计奖惩分布情况

### 2.7 创新活动管理
- **创新项目管理**：支持学生参与创新项目管理
- **创新成果记录**：记录学生取得的创新成果
- **活动参与管理**：管理学生参加各类活动的情况

### 2.8 财务管理
- **缴费管理**：记录学生缴费相关信息

### 2.9 统计分析
- **多维度统计**：班级人数统计、课程平均分统计、考勤率统计、请假通过率统计等
- **主页数据**：为不同角色用户提供个性化统计数据
- **报表生成**：支持生成各类统计报表

## 3. 非功能需求

### 3.1 性能需求
- **响应时间**：系统在正常负载下，页面响应时间不超过3秒
- **并发处理**：支持至少100个用户同时在线使用
- **数据处理能力**：能够处理大量学生数据的批量导入导出
- **系统可用性**：系统可用性达到99%

### 3.2 安全需求
- **身份认证**：使用JWT令牌进行身份认证，确保用户身份合法性
- **权限控制**：基于角色的访问控制，防止越权访问
- **数据加密**：敏感数据（如密码）使用BCrypt加密存储
- **输入验证**：对所有用户输入进行验证，防止SQL注入和XSS攻击
- **数据备份**：定期备份重要数据，确保数据安全

### 3.3 可靠性需求
- **错误处理**：系统应具备完善的错误处理机制
- **数据一致性**：通过数据库事务保证数据一致性
- **异常恢复**：系统在出现异常时能够快速恢复

### 3.4 可维护性需求
- **代码规范**：遵循统一的编码规范，便于后期维护
- **文档完整**：提供完整的系统设计和使用文档
- **模块化设计**：采用模块化设计，降低模块间耦合度

### 3.5 可扩展性需求
- **架构设计**：采用可扩展的架构设计，支持功能扩展
- **接口设计**：提供标准化的API接口，便于第三方集成

### 3.6 易用性需求
- **界面友好**：提供直观友好的用户界面
- **操作简便**：简化操作流程，降低用户学习成本
- **提示信息**：提供清晰的操作提示和错误信息

## 4. 系统架构

### 4.1 技术架构
本系统采用Spring Boot + Spring Security + Spring Data JPA + MySQL的四层架构模式：

```
┌─────────────────┐
│   前端界面层    │  ← JavaFX/FXML
├─────────────────┤
│   控制器层      │  ← Spring MVC Controller
├─────────────────┤
│   业务逻辑层    │  ← Service Layer
├─────────────────┤
│   数据访问层    │  ← Repository/JPA
├─────────────────┤
│   数据库层      │  ← MySQL 8.0
└─────────────────┘
```

### 4.2 技术选型
- **后端框架**：Spring Boot 3.4.2
- **持久层**：Spring Data JPA
- **安全框架**：Spring Security
- **数据库**：MySQL 8.0
- **缓存**：Redis
- **邮件服务**：Spring Mail
- **JSON处理**：Jackson
- **JWT认证**：JJwt
- **Excel处理**：Apache POI
- **开发语言**：Java 21
- **构建工具**：Maven

### 4.3 系统模块划分
- **用户管理模块**：负责用户注册、登录、权限管理
- **学生信息模块**：负责学生基本信息管理
- **教学管理模块**：负责课程、课表、成绩管理
- **考勤管理模块**：负责考勤记录和统计
- **请假审批模块**：负责请假申请和审批流程
- **奖惩管理模块**：负责荣誉和处分记录管理
- **创新活动模块**：负责创新项目和活动管理
- **统计分析模块**：负责数据统计和报表生成

## 5. 实体关系

### 5.1 核心实体
#### 5.1.1 person（人员表）
- **功能**：存储所有人员的基本信息（管理员、学生、教师）
- **属性**：person_id（主键）、num（编号）、name（姓名）、type（类型）、dept（学院/部门）、card（身份证号）、gender（性别）、birthday（生日）、email（邮箱）、phone（电话）、address（地址）、introduce（个人简介）、photo（照片）

#### 5.1.2 student（学生表）
- **功能**：存储学生特有的信息
- **属性**：person_id（主键，外键关联person表）、major（专业）、className（班级）

#### 5.1.3 user（用户表）
- **功能**：存储用户的登录信息和权限
- **属性**：person_id（主键，外键关联person表）、user_type_id（用户类型ID）、userName（用户名）、password（密码）、loginCount（登录次数）、lastLoginTime（最后登录时间）、createTime（创建时间）、creatorId（创建者ID）

#### 5.1.4 menu（菜单表）
- **功能**：存储系统菜单结构
- **属性**：id（主键）、pid（父菜单ID，自关联）、userTypeIds（可见用户类型ID列表）、name（菜单名）、title（菜单标题）

#### 5.1.5 dictionary（数据字典表）
- **功能**：存储系统通用数据字典
- **属性**：id（主键）、pid（父字典ID，自关联）、value（字典值）、label（字典标签）

#### 5.1.6 user_type（用户类型表）
- **功能**：定义系统用户角色类型
- **属性**：id（主键）、name（角色名称：ROLE_ADMIN、ROLE_STUDENT、ROLE_TEACHER）

### 5.2 扩展实体

#### 5.2.1 teacher（教师表）
- **功能**：存储教师特有信息
- **属性**：person_id（主键，外键关联person表）、title（职称）、degree（学位）

#### 5.2.2 course（课程表）
- **功能**：存储课程信息
- **属性**：course_id（主键）、num（课程编号）、name（课程名称）、credit（学分）、pre_course_id（前驱课程ID，自关联）、coursePath（课程路径）

#### 5.2.3 course_schedule（课表安排表）
- **功能**：存储课程时间安排
- **属性**：schedule_id（主键）、course_id（课程ID）、teacher_id（教师ID）、className（上课班级）、semester（学期）、dayOfWeek（星期几）、startPeriod（开始节次）、endPeriod（结束节次）、location（上课地点）、status（状态）

#### 5.2.4 score（成绩表）
- **功能**：存储学生成绩信息
- **属性**：score_id（主键）、person_id（学生ID，外键）、course_id（课程ID，外键）、mark（成绩）、ranking（排名）

#### 5.2.5 attendance（考勤表）
- **功能**：存储学生考勤记录
- **属性**：attendance_id（主键）、person_id（学生ID，外键）、course_id（课程ID，外键）、state（状态：0-未到，1-迟到，2-早退，3-正常，4-请假）、attendanceTime（考勤时间）、remark（备注）

#### 5.2.6 student_leave（请假表）
- **功能**：存储学生请假申请信息
- **属性**：student_leave_id（主键）、student_id（学生ID，外键）、teacher_id（审批教师ID，外键）、leaveDate（请假日期）、reason（请假原因）、state（状态：0-待审批，1-通过，2-驳回）、applyTime（申请时间）、teacherComment（教师审批意见）、teacherTime（教师审批时间）、adminComment（管理员审批意见）、adminTime（管理员审批时间）、leaveType（请假类型）、leaveDuration（请假时长）

#### 5.2.7 family_member（家庭成员表）
- **功能**：存储学生家庭成员信息
- **属性**：member_id（主键）、person_id（学生ID，外键）、relation（关系）、name（姓名）、gender（性别）、age（年龄）、unit（工作单位）

#### 5.2.8 fee（消费流水表）
- **功能**：存储学生消费记录
- **属性**：fee_id（主键）、person_id（学生ID，外键）、day（日期）、money（金额）、type（类型：1-学费缴纳，2-校园卡消费）

#### 5.2.9 honor_record（荣誉记录表）
- **功能**：存储学生荣誉奖励信息
- **属性**：honor_id（主键）、person_id（学生ID）、title（奖项名称）、level（级别）、category（类别）、description（描述）、awardDate（获奖日期）、issuer（颁发机构）、recorderId（录入人ID）、status（状态）、createTime（创建时间）、certificateUrl（证书路径）

#### 5.2.10 punishment_record（处分记录表）
- **功能**：存储学生违纪处分信息
- **属性**：punishment_id（主键）、person_id（学生ID）、behavior（违纪行为）、level（处分级别）、description（详情）、behaviorDate（违纪日期）、location（地点）、recorderId（录入人ID）、status（状态）、createTime（创建时间）

#### 5.2.11 innovation_project（创新项目表）
- **功能**：存储创新项目管理信息
- **属性**：project_id（主键）、name（项目名称）、category（类别）、leaderId（负责人ID）、teamMembers（团队成员）、description（描述）、status（状态：0-进行中，1-已完成，2-已终止）、startDate（开始日期）、endDate（结束日期）、createTime（创建时间）

#### 5.2.12 innovation_achievement（创新成果表）
- **功能**：存储创新项目成果信息
- **属性**：achievement_id（主键）、project_id（项目ID，外键）、person_id（完成人ID）、title（成果标题）、content（内容）、achievementType（成果类型）、submitTime（提交时间）

#### 5.2.13 activity（活动表）
- **功能**：存储校园活动信息
- **属性**：activity_id（主键）、name（活动名称）、location（地点）、startTime（开始时间）、endTime（结束时间）、description（描述）、publisherId（发布者ID）、status（状态：0-未开始，1-进行中，2-已结束，3-已取消）、createTime（创建时间）

#### 5.2.14 activity_participation（活动参与表）
- **功能**：存储学生活动参与记录
- **属性**：participation_id（主键）、activity_id（活动ID，外键）、person_id（参与者ID）、joinTime（参与时间）、remark（备注）

### 5.3 实体关系

**核心关系**：
- **person - student**：一对一关系（@OneToOne），person表为主表，student表为从表，通过person_id关联
- **person - user**：一对一关系（@OneToOne），person表为主表，user表为从表，通过person_id关联
- **person - teacher**：一对一关系（@OneToOne），person表为主表，teacher表为从表，通过person_id关联
- **user - user_type**：多对一关系（@ManyToOne），一个用户类型对应多个用户

**教学相关关系**：
- **student - score**：一对多关系，一个学生有多门课程的成绩，通过person_id关联
- **course - score**：一对多关系，一门课程有多个学生的成绩，通过course_id关联
- **student - attendance**：一对多关系，一个学生有多条考勤记录，通过person_id关联
- **course - attendance**：一对多关系，一门课程有多条考勤记录，通过course_id关联
- **course - course_schedule**：一对多关系，一门课程可以有多个时间安排
- **teacher - course_schedule**：一对多关系，一个教师可以教授多个课程安排
- **course - course**：自关联关系，通过pre_course_id实现课程前置关系

**请假相关关系**：
- **student - student_leave**：一对多关系，一个学生可以有多次请假申请，通过student_id关联
- **teacher - student_leave**：一对多关系，一个教师可以审批多次请假申请，通过teacher_id关联

**其他关系**：
- **student - family_member**：一对多关系，一个学生可以有多个家庭成员，通过person_id关联
- **student - fee**：一对多关系，一个学生可以有多条消费记录，通过person_id关联
- **person - honor_record**：一对多关系，一个人可以获得多个荣誉，通过person_id关联
- **person - punishment_record**：一对多关系，一个人可能有多条处分记录，通过person_id关联
- **innovation_project - innovation_achievement**：一对多关系，一个项目可以有多个成果
- **activity - activity_participation**：一对多关系，一个活动可以有多个参与者

## 6. 数据库设计

### 6.1 数据库概览
- **数据库名**：java_3_10
- **数据库类型**：MySQL 8.0
- **字符集**：utf8mb4（支持中文和特殊字符）
- **存储引擎**：InnoDB（支持事务和外键）
- **主键策略**：自增 INT（AUTO_INCREMENT）
- **外键约束**：级联删除/更新（ON DELETE CASCADE / ON UPDATE CASCADE）
- **ORM框架**：Spring Data JPA（ddl-auto: update模式，表结构由JPA自动生成）

### 6.2 核心表设计

#### 6.2.1 person（人员表）
```sql
CREATE TABLE person (
                        person_id INT AUTO_INCREMENT PRIMARY KEY,
                        num VARCHAR(20) UNIQUE NOT NULL COMMENT '人员编号（学号/工号）',
                        name VARCHAR(50) NULL COMMENT '姓名',
                        type VARCHAR(2) NULL COMMENT '人员类型：0-管理员，1-学生，2-教师',
                        dept VARCHAR(50) NULL COMMENT '学院/部门',
                        card VARCHAR(20) NULL COMMENT '身份证号',
                        gender VARCHAR(2) NULL COMMENT '性别：1-男，2-女',
                        birthday VARCHAR(10) NULL COMMENT '出生日期',
                        email VARCHAR(60) NULL COMMENT '邮箱',
                        phone VARCHAR(20) NULL COMMENT '电话',
                        address VARCHAR(200) NULL COMMENT '地址',
                        introduce VARCHAR(1000) NULL COMMENT '个人简介',
                        photo LONGBLOB NULL COMMENT '照片'
);
CREATE UNIQUE INDEX uk_person_num ON person(num);
```

#### 6.2.2 student（学生表）
```sql
CREATE TABLE student (
                         person_id INT PRIMARY KEY,
                         major VARCHAR(20) NULL COMMENT '专业',
                         class_name VARCHAR(50) NULL COMMENT '班级',
                         FOREIGN KEY (person_id) REFERENCES person(person_id) ON DELETE CASCADE
);
CREATE INDEX idx_class_name ON student(class_name);
```

#### 6.2.3 user（用户表）
```sql
CREATE TABLE user (
                      person_id INT PRIMARY KEY,
                      user_type_id INT NULL COMMENT '用户类型ID',
                      user_name VARCHAR(20) UNIQUE NOT NULL COMMENT '登录账号',
                      password VARCHAR(100) NOT NULL COMMENT '密码（BCrypt加密）',
                      login_count INT DEFAULT 0 COMMENT '登录次数',
                      last_login_time VARCHAR(20) NULL COMMENT '最后登录时间',
                      create_time VARCHAR(20) NULL COMMENT '创建时间',
                      creator_id INT NULL COMMENT '创建者ID',
                      FOREIGN KEY (person_id) REFERENCES person(person_id) ON DELETE CASCADE
);
CREATE UNIQUE INDEX uk_user_username ON user(user_name);
```

#### 6.2.4 menu（菜单表）
```sql
CREATE TABLE menu (
                      id INT PRIMARY KEY,
                      pid INT NULL COMMENT '父菜单ID（自关联）',
                      user_type_ids VARCHAR(255) NULL COMMENT '可见用户类型ID列表',
                      name VARCHAR(40) NULL COMMENT '菜单名（FXML文件名）',
                      title VARCHAR(40) NULL COMMENT '菜单标题',
                      FOREIGN KEY (pid) REFERENCES menu(id) ON DELETE CASCADE
);
```

#### 6.2.5 dictionary（数据字典表）
```sql
CREATE TABLE dictionary (
                            id INT AUTO_INCREMENT PRIMARY KEY,
                            pid INT NULL COMMENT '父字典ID（自关联）',
                            value VARCHAR(40) NULL COMMENT '字典值',
                            label VARCHAR(40) NULL COMMENT '字典标签',
                            FOREIGN KEY (pid) REFERENCES dictionary(id) ON DELETE CASCADE
);
```

### 6.3 扩展表设计

#### 6.3.1 course（课程表）
```sql
CREATE TABLE course (
                        course_id INT AUTO_INCREMENT PRIMARY KEY,
                        num VARCHAR(20) UNIQUE NULL COMMENT '课程编号',
                        name VARCHAR(50) NULL COMMENT '课程名称',
                        credit INT NULL COMMENT '学分',
                        pre_course_id INT NULL COMMENT '前驱课程ID，自关联',
                        course_path VARCHAR(12) NULL COMMENT '课程路径',
                        FOREIGN KEY (pre_course_id) REFERENCES course(course_id)
);
CREATE INDEX idx_course_num ON course(num);
CREATE INDEX idx_course_name ON course(name);
CREATE INDEX idx_course_pre_course_id ON course(pre_course_id);
```

#### 6.3.2 course_schedule（课表安排表）
```sql
CREATE TABLE course_schedule (
                                 schedule_id INT AUTO_INCREMENT PRIMARY KEY,
                                 course_id INT NOT NULL COMMENT '课程ID',
                                 teacher_id INT NOT NULL COMMENT '教师ID',
                                 class_name VARCHAR(50) NOT NULL COMMENT '上课班级',
                                 semester VARCHAR(20) NOT NULL COMMENT '学期',
                                 day_of_week INT NOT NULL COMMENT '星期几（1-7表示周一到周日）',
                                 start_period INT NOT NULL COMMENT '开始节次',
                                 end_period INT NOT NULL COMMENT '结束节次',
                                 location VARCHAR(100) NOT NULL COMMENT '上课地点',
                                 status INT DEFAULT 1 COMMENT '状态（1-正常，2-停用）',
                                 FOREIGN KEY (course_id) REFERENCES course(course_id),
                                 FOREIGN KEY (teacher_id) REFERENCES teacher(person_id)
);
CREATE INDEX idx_schedule_class ON course_schedule(class_name);
CREATE INDEX idx_schedule_semester ON course_schedule(semester);
```

#### 6.3.3 score（成绩表）
```sql
CREATE TABLE score (
                       score_id INT AUTO_INCREMENT PRIMARY KEY,
                       person_id INT NOT NULL COMMENT '学生ID',
                       course_id INT NOT NULL COMMENT '课程ID',
                       mark INT NULL COMMENT '成绩',
                       ranking INT NULL COMMENT '排名',
                       FOREIGN KEY (person_id) REFERENCES student(person_id),
                       FOREIGN KEY (course_id) REFERENCES course(course_id)
);
CREATE INDEX idx_score_person_id ON score(person_id);
CREATE INDEX idx_score_course_id ON score(course_id);
CREATE INDEX idx_score_mark ON score(mark);
```

#### 6.3.4 attendance（考勤表）
```sql
CREATE TABLE attendance (
                            attendance_id INT AUTO_INCREMENT PRIMARY KEY,
                            person_id INT NOT NULL COMMENT '学生ID',
                            course_id INT NOT NULL COMMENT '课程ID',
                            attendance_time DATETIME NULL COMMENT '考勤时间',
                            state INT NULL COMMENT '状态：0-未到，1-迟到，2-早退，3-正常，4-请假',
                            remark VARCHAR(200) NULL COMMENT '备注',
                            FOREIGN KEY (person_id) REFERENCES student(person_id),
                            FOREIGN KEY (course_id) REFERENCES course(course_id)
);
CREATE INDEX idx_attendance_person_id ON attendance(person_id);
CREATE INDEX idx_attendance_course_id ON attendance(course_id);
CREATE INDEX idx_attendance_state ON attendance(state);
```

#### 6.3.5 student_leave（请假表）
```sql
CREATE TABLE student_leave (
                               student_leave_id INT AUTO_INCREMENT PRIMARY KEY,
                               student_id INT NOT NULL COMMENT '学生ID',
                               teacher_id INT NULL COMMENT '审批教师ID',
                               leave_date VARCHAR(50) NULL COMMENT '请假日期',
                               reason VARCHAR(100) NULL COMMENT '请假原因',
                               state INT DEFAULT 0 COMMENT '状态：0-待审批，1-通过，2-驳回',
                               apply_time DATETIME NULL COMMENT '申请时间',
                               teacher_comment VARCHAR(100) NULL COMMENT '教师审批意见',
                               teacher_time DATETIME NULL COMMENT '教师审批时间',
                               admin_comment VARCHAR(100) NULL COMMENT '管理员审批意见',
                               admin_time DATETIME NULL COMMENT '管理员审批时间',
                               leave_type VARCHAR(20) NULL COMMENT '请假类型：病假/事假',
                               leave_duration INT NULL COMMENT '请假时长（小时）',
                               FOREIGN KEY (student_id) REFERENCES student(person_id) ON DELETE CASCADE,
                               FOREIGN KEY (teacher_id) REFERENCES teacher(person_id) ON DELETE SET NULL
);
CREATE INDEX idx_student_leave_student_id ON student_leave(student_id);
CREATE INDEX idx_student_leave_state ON student_leave(state);
CREATE INDEX idx_student_leave_teacher_id ON student_leave(teacher_id);
CREATE INDEX idx_leave_date ON student_leave(leave_date);
```

#### 6.3.6 teacher（教师表）
```sql
CREATE TABLE teacher (
                         person_id INT PRIMARY KEY COMMENT '人员ID，关联person表',
                         title VARCHAR(20) NULL COMMENT '职称',
                         degree VARCHAR(10) NULL COMMENT '学位',
                         FOREIGN KEY (person_id) REFERENCES person(person_id) ON DELETE CASCADE
);
```

#### 6.3.7 punishment_record（处分记录表）
```sql
CREATE TABLE punishment_record (
                                   punishment_id INT AUTO_INCREMENT PRIMARY KEY COMMENT '处分记录ID',
                                   person_id INT NOT NULL COMMENT '违纪学生ID，关联person表',
                                   behavior VARCHAR(200) NULL COMMENT '违纪行为描述',
                                   level VARCHAR(100) NULL COMMENT '处分级别（警告/严重警告/记过/留校察看/开除学籍）',
                                   description TEXT NULL COMMENT '违纪详情',
                                   behavior_date DATE NULL COMMENT '违纪日期',
                                   location VARCHAR(200) NULL COMMENT '违纪地点',
                                   recorder_id INT NULL COMMENT '记录人ID，关联person表',
                                   status INT DEFAULT 0 COMMENT '状态：0=待处理，1=已处理',
                                   create_time DATETIME NULL COMMENT '创建时间',
                                   FOREIGN KEY (person_id) REFERENCES person(person_id) ON DELETE CASCADE,
                                   FOREIGN KEY (recorder_id) REFERENCES person(person_id) ON DELETE SET NULL
);
CREATE INDEX idx_punishment_person ON punishment_record(person_id);
CREATE INDEX idx_punishment_status ON punishment_record(status);
CREATE INDEX idx_punishment_level ON punishment_record(level);
CREATE INDEX idx_punishment_date ON punishment_record(behavior_date);
```

#### 6.3.8 punishment_result（处分处理结果表）
```sql
CREATE TABLE punishment_result (
                                   result_id INT AUTO_INCREMENT PRIMARY KEY COMMENT '处理结果ID',
                                   punishment_id INT NOT NULL COMMENT '关联处分记录ID',
                                   result_type VARCHAR(100) NULL COMMENT '处理类型（通报批评/记过处分/留校察看/开除学籍等）',
                                   description TEXT NULL COMMENT '处理详情/依据',
                                   result_date DATE NULL COMMENT '处理决定日期',
                                   handler VARCHAR(200) NULL COMMENT '处理人/处理部门',
                                   create_time DATETIME NULL COMMENT '创建时间',
                                   FOREIGN KEY (punishment_id) REFERENCES punishment_record(punishment_id) ON DELETE CASCADE
);
CREATE INDEX idx_result_punishment ON punishment_result(punishment_id);
```

#### 6.3.9 activity_participation（活动参与表）
```sql
CREATE TABLE activity_participation (
                                        participation_id INT AUTO_INCREMENT PRIMARY KEY COMMENT '参与记录ID',
                                        activity_id INT NOT NULL COMMENT '活动ID，关联activity表',
                                        person_id INT NOT NULL COMMENT '参与者ID，关联person表',
                                        join_time DATETIME NULL COMMENT '参与时间',
                                        remark VARCHAR(200) NULL COMMENT '备注',
                                        FOREIGN KEY (activity_id) REFERENCES activity(activity_id) ON DELETE CASCADE,
                                        FOREIGN KEY (person_id) REFERENCES person(person_id) ON DELETE CASCADE,
                                        UNIQUE KEY uk_participation_activity_person (activity_id, person_id)
);
CREATE INDEX idx_participation_activity ON activity_participation(activity_id);
CREATE INDEX idx_participation_person ON activity_participation(person_id);
```

#### 6.3.10 statistics_day（每日统计数据表）
```sql
CREATE TABLE statistics_day (
                                day VARCHAR(10) NOT NULL PRIMARY KEY COMMENT '日期（YYYY-MM-DD）',
                                login_count INT DEFAULT 0 COMMENT '登录次数',
                                request_count INT DEFAULT 0 COMMENT '请求次数',
                                create_count INT DEFAULT 0 COMMENT '创建次数',
                                modify_count INT DEFAULT 0 COMMENT '修改次数'
);
```

#### 6.3.11 student_statistics（学生统计表）
```sql
CREATE TABLE student_statistics (
                                    statistics_id INT AUTO_INCREMENT PRIMARY KEY COMMENT '统计ID',
                                    personId INT NOT NULL COMMENT '学生ID，关联student表',
                                    year VARCHAR(4) NULL COMMENT '学年',
                                    course_count INT DEFAULT 0 COMMENT '课程数量',
                                    avg_score DECIMAL(5,2) NULL COMMENT '平均分',
                                    gpa DECIMAL(3,2) NULL COMMENT '绩点',
                                    leave_count INT DEFAULT 0 COMMENT '请假次数',
                                    no INT NULL COMMENT '排序号',
                                    FOREIGN KEY (personId) REFERENCES student(person_id) ON DELETE CASCADE
);
CREATE INDEX idx_statistics_student ON student_statistics(personId);
CREATE INDEX idx_statistics_year ON student_statistics(year);
```

#### 6.3.12 modify_log（修改日志表）
```sql
CREATE TABLE modify_log (
                            id INT AUTO_INCREMENT PRIMARY KEY COMMENT '日志ID',
                            type VARCHAR(4) NULL COMMENT '操作类型',
                            table_name VARCHAR(20) NULL COMMENT '涉及表名',
                            info VARCHAR(2000) NULL COMMENT '详细信息',
                            operate_time VARCHAR(20) NULL COMMENT '操作时间',
                            operator_id INT NULL COMMENT '操作员ID，关联person表',
                            FOREIGN KEY (operator_id) REFERENCES person(person_id) ON DELETE SET NULL
);
```

#### 6.3.13 request_log（请求日志表）
```sql
CREATE TABLE request_log (
                             id INT AUTO_INCREMENT PRIMARY KEY COMMENT '日志ID',
                             url VARCHAR(100) NULL COMMENT '请求URL',
                             username VARCHAR(20) NULL COMMENT '用户名',
                             start_time VARCHAR(20) NULL COMMENT '开始时间',
                             request_time DOUBLE NULL COMMENT '请求耗时（秒）'
);
```

#### 6.3.14 system_info（系统信息表）
```sql
CREATE TABLE system_info (
                             id INT AUTO_INCREMENT PRIMARY KEY COMMENT '信息ID',
                             name VARCHAR(40) NULL COMMENT '配置项名称',
                             value VARCHAR(40) NULL COMMENT '配置项值',
                             des VARCHAR(100) NULL COMMENT '描述'
);
```

### 6.4 索引设计

#### 6.4.1 统计查询索引

| 索引名 | 表名 | 字段 | 用途 |
|--------|------|------|------|
| idx_student_classname | student | class_name | 班级学生数统计 |
| idx_score_course_id | score | course_id | 课程平均分统计 |
| idx_attendance_course_id | attendance | course_id | 课程考勤率统计 |
| idx_student_leave_student_id | student_leave | student_id | 请假统计 |
| idx_student_leave_state | student_leave | state | 请假通过率统计 |

#### 6.4.2 常用查询索引

| 索引名 | 表名 | 字段 | 用途 |
|--------|------|------|------|
| uk_person_num | person | num | 按学号/工号查询 |
| uk_user_username | user | user_name | 按用户名查询 |
| idx_score_person_id | score | person_id | 按学生查成绩 |
| idx_attendance_person_id | attendance | person_id | 按学生查考勤 |

## 7. 后端接口设计

### 7.1 接口设计原则
- **RESTful风格**：遵循RESTful API设计规范
- **统一数据格式**：使用统一的请求和响应数据格式
- **权限验证**：所有接口都经过身份验证和权限校验
- **错误处理**：统一的错误码和错误信息返回

### 7.2 通用数据结构

#### 7.2.1 请求数据结构
```json
{
  "data": {},
  "page": {
    "pageNum": 1,
    "pageSize": 10
  },
  "sort": {
    "field": "createTime",
    "order": "desc"
  }
}
```

#### 7.2.2 响应数据结构
```json
{
  "success": true,
  "code": 200,
  "message": "操作成功",
  "data": {},
  "total": 0
}
```

### 7.3 认证接口

#### 7.3.1 用户登录
- **URL**：POST /auth/login
- **请求参数**：
```json
{
  "username": "admin",
  "password": "password"
}
```

- **响应示例**：
```json
{
  "success": true,
  "code": 200,
  "message": "登录成功",
  "data": {
    "token": "eyJhbGciOiJIUzUxMiJ9...",
    "user": {
      "personId": 1,
      "username": "admin",
      "name": "管理员",
      "type": "0"
    }
  }
}
```

#### 7.3.2 获取验证码
- **URL**：POST /auth/getValidateCode
- **响应示例**：
```json
{
  "success": true,
  "code": 200,
  "message": "获取验证码成功",
  "data": {
    "validateCode": "abc123",
    "image": "base64..."
  }
}
```

#### 7.3.3 用户注册
- **URL**：POST /auth/registerUser
- **请求参数**：
```json
{
  "data": {
    "username": "student001",
    "password": "123456",
    "name": "张三",
    "type": "1",
    "num": "20200001"
  }
}
```

#### 7.3.4 修改密码
- **URL**：POST /auth/changePassword
- **请求参数**：
```json
{
  "data": {
    "oldPassword": "oldpwd",
    "newPassword": "newpwd"
  }
}
```

#### 7.3.5 获取验证码
- **URL**：POST /auth/getValidateCode
- **响应示例**：
```json
{
  "success": true,
  "code": 200,
  "message": "获取验证码成功",
  "data": {
    "validateCode": "abc123",
    "image": "base64..."
  }
}
```

#### 7.3.6 验证信息
- **URL**：POST /auth/testValidateInfo
- **请求参数**：
```json
{
  "data": {
    "validateCode": "abc123",
    "inputCode": "abc12"
  }
}
```

#### 7.3.7 退出登录
- **URL**：POST /auth/logout
- **响应示例**：
```json
{
  "success": true,
  "code": 200,
  "message": "退出登录成功",
  "data": {}
}
```

#### 7.3.8 生成加密密码
- **URL**：GET /auth/generatePassword?password=123456
- **响应示例**：
```json
{
  "success": true,
  "code": 200,
  "message": "生成密码成功",
  "data": {
    "encryptedPassword": "$2a$10$vdtl6.."
  }
}
```

### 7.4 学生管理接口

#### 7.4.1 查询学生列表
- **URL**：POST /api/student/getStudentList
- **权限**：ADMIN, STUDENT, TEACHER
- **请求参数**：
```json
{
  "data": {
    "numName": "张三" // 按学号或姓名搜索
  },
  "page": {
    "pageNum": 1,
    "pageSize": 10
  }
}
```

#### 7.4.2 删除学生
- **URL**：POST /api/student/studentDelete
- **权限**：ADMIN
- **请求参数**：
```json
{
  "data": {
    "personId": 1
  }
}
```

#### 7.4.3 获取学生详情
- **URL**：POST /api/student/getStudentInfo
- **权限**：ADMIN
- **请求参数**：
```json
{
  "data": {
    "personId": 1
  }
}
```

#### 7.4.4 保存学生信息
- **URL**：POST /api/student/studentEditSave
- **权限**：ADMIN
- **请求参数**：
```json
{
  "data": {
    "personId": null, // 新增时为null
    "num": "20200001",
    "name": "张三",
    "type": "1",
    "major": "计算机科学与技术",
    "className": "软件工程2020级1班"
  }
}
```

#### 7.4.5 查询家庭成员
- **URL**：POST /api/student/getFamilyMemberList
- **权限**：ADMIN, STUDENT
- **请求参数**：
```json
{
  "data": {
    "studentId": 1
  }
}
```

#### 7.4.6 导入消费流水数据
- **URL**：POST /api/student/importFeeData
- **权限**：ADMIN
- **请求参数**：
```json
{
  "data": {
    "personId": 1,
    "uploader": "admin",
    "file": "base64 encoded file data"
  }
}
```

#### 7.4.7 导出学生列表Excel
- **URL**：POST /api/student/getStudentListExcl
- **权限**：ADMIN
- **请求参数**：
```json
{
  "data": {
    "numName": "张三" // 按学号或姓名搜索
  }
}
```

#### 7.4.8 获取学生页面数据
- **URL**：POST /api/student/getStudentPageData
- **权限**：ADMIN
- **请求参数**：
```json
{
  "data": {
    "personId": 1
  }
}
```

#### 7.4.9 保存家庭成员
- **URL**：POST /api/student/familyMemberSave
- **权限**：ADMIN, STUDENT
- **请求参数**：
```json
{
  "data": {
    "memberId": null, // 新增时为null
    "studentId": 1,
    "relation": "父亲",
    "name": "张父",
    "gender": "男",
    "age": 45,
    "unit": "某公司"
  }
}
```

#### 7.4.10 删除家庭成员
- **URL**：POST /api/student/familyMemberDelete
- **权限**：ADMIN, STUDENT
- **请求参数**：
```json
{
  "data": {
    "memberId": 1
  }
}
```

#### 7.4.11 Web导入消费数据
- **URL**：POST /api/student/importFeeDataWeb
- **权限**：STUDENT
- **请求参数**：
```json
{
  "personId": 1,
  "file": "multipart file"
}
```

#### 7.4.12 获取学生介绍数据
- **URL**：POST /api/student/getStudentIntroduceData
- **权限**：STUDENT, ADMIN
- **请求参数**：
```json
{
  "data": {
    "personId": 1
  }
}
```

### 7.5 请假管理接口

#### 7.5.1 查询请假列表
- **URL**：POST /api/studentLeave/getStudentLeaveList
- **权限**：ADMIN, TEACHER（查询所有），STUDENT（仅本人）
- **请求参数**：
```json
{
  "data": {
    "studentId": 1, // 学生ID，可选
    "state": 0 // 状态筛选，可选
  },
  "page": {
    "pageNum": 1,
    "pageSize": 10
  }
}
```

#### 7.5.2 学生提交请假申请
- **URL**：POST /api/studentLeave/studentLeaveApply
- **权限**：STUDENT
- **请求参数**：
```json
{
  "data": {
    "studentId": 1,
    "leaveDate": "2026-06-03",
    "reason": "生病需要就医",
    "leaveType": "病假",
    "leaveDuration": 4 // 请假时长（小时）
  }
}
```

#### 7.5.3 请假审批
- **URL**：POST /api/studentLeave/studentLeaveApprove
- **权限**：ADMIN, TEACHER
- **请求参数**：
```json
{
  "data": {
    "studentLeaveId": 1,
    "state": 1, // 1-通过，2-驳回
    "comment": "同意请假"
  }
}
```

### 7.6 教学管理接口

#### 7.6.1 查询课程列表
- **URL**：POST /api/course/getCourseList
- **权限**：无限制
- **请求参数**：
```json
{
  "data": {
    "name": "高等数学" // 课程名称搜索
  },
  "page": {
    "pageNum": 1,
    "pageSize": 10
  }
}
```

#### 7.6.2 保存课程信息
- **URL**：POST /api/course/courseSave
- **权限**：ADMIN
- **请求参数**：
```json
{
  "data": {
    "courseId": null, // 新增时为null
    "name": "高等数学",
    "credit": 4,
    "teacherId": 1
  }
}
```

#### 7.6.3 删除课程
- **URL**：POST /api/course/courseDelete
- **权限**：ADMIN
- **请求参数**：
```json
{
  "data": {
    "courseId": 1
  }
}
```

### 7.7 成绩管理接口

#### 7.7.1 获取学生选项列表
- **URL**：POST /api/score/getStudentItemOptionList
- **权限**：ADMIN, TEACHER
- **请求参数**：
```json
{
  "data": {
    "courseId": 1
  }
}
```

#### 7.7.2 获取课程选项列表
- **URL**：POST /api/score/getCourseItemOptionList
- **权限**：ADMIN, TEACHER
- **请求参数**：
```json
{
  "data": {
    "studentId": 1
  }
}
```

#### 7.7.3 查询成绩列表
- **URL**：POST /api/score/getScoreList
- **权限**：ADMIN, TEACHER, STUDENT
- **请求参数**：
```json
{
  "data": {
    "studentId": 1,
    "courseId": 1
  }
}
```

#### 7.7.4 保存成绩
- **URL**：POST /api/score/scoreSave
- **权限**：ADMIN, TEACHER
- **请求参数**：
```json
{
  "data": {
    "scoreId": null, // 新增时为null
    "studentId": 1,
    "courseId": 1,
    "mark": 95
  }
}
```

#### 7.7.5 删除成绩
- **URL**：POST /api/score/scoreDelete
- **权限**：ADMIN, TEACHER
- **请求参数**：
```json
{
  "data": {
    "scoreId": 1
  }
}
```

#### 7.7.6 导出成绩Excel
- **URL**：POST /api/score/exportExcel
- **权限**：ADMIN, TEACHER
- **请求参数**：
```json
{
  "data": {
    "courseId": 1
  }
}
```

### 7.8 考勤管理接口

#### 7.8.1 查询考勤列表
- **URL**：POST /api/attendance/getAttendanceList
- **权限**：ADMIN, TEACHER, STUDENT
- **请求参数**：
```json
{
  "data": {
    "studentId": 1,
    "courseId": 1
  }
}
```

#### 7.8.2 保存考勤
- **URL**：POST /api/attendance/attendanceSave
- **权限**：ADMIN, TEACHER
- **请求参数**：
```json
{
  "data": {
    "attendanceId": null, // 新增时为null
    "studentId": 1,
    "courseId": 1,
    "state": 3, // 0-未到，1-迟到，2-早退，3-正常，4-请假
    "attendanceTime": "2026-06-06T08:00:00",
    "remark": "正常出勤"
  }
}
```

#### 7.8.3 删除考勤记录
- **URL**：POST /api/attendance/attendanceDelete
- **权限**：ADMIN, TEACHER
- **请求参数**：
```json
{
  "data": {
    "attendanceId": 1
  }
}
```

### 7.9 荣誉管理接口

#### 7.9.1 录入学生奖项
- **URL**：POST /api/honor/addHonor
- **权限**：ADMIN, TEACHER
- **请求参数**：
```json
{
  "data": {
    "personId": 1,
    "title": "优秀学生奖",
    "level": "校级",
    "category": "学术",
    "description": "表彰学习成绩优异",
    "awardDate": "2026-06-06",
    "issuer": "山东大学",
    "certificateUrl": "/uploads/certificates/award1.jpg"
  }
}
```

#### 7.9.2 查询荣誉列表
- **URL**：POST /api/honor/list
- **权限**：ADMIN可查全部，STUDENT仅可查自己
- **请求参数**：
```json
{
  "data": {
    "personId": 1, // 学生ID，可选
    "level": "校级"  // 级别筛选，可选
  }
}
```

### 7.10 处分管理接口

#### 7.10.1 记录违纪行为
- **URL**：POST /api/punishment/addPunishment
- **权限**：ADMIN
- **请求参数**：
```json
{
  "data": {
    "personId": 1,
    "behavior": "考试作弊",
    "level": "记过",
    "description": "在期末考试中作弊",
    "behaviorDate": "2026-06-06",
    "location": "教学楼A-201"
  }
}
```

#### 7.10.2 添加处理结果
- **URL**：POST /api/punishment/addResult
- **权限**：ADMIN
- **请求参数**：
```json
{
  "data": {
    "punishmentId": 1,
    "result": "留校察看半年",
    "handler": "教务处"
  }
}
```

#### 7.10.3 查询处分列表
- **URL**：POST /api/punishment/list
- **权限**：ADMIN可查全部，STUDENT仅可查自己
- **请求参数**：
```json
{
  "data": {
    "personId": 1, // 学生ID，可选
    "status": 1  // 状态筛选，可选
  }
}
```

#### 7.10.4 撤销处分
- **URL**：POST /api/punishment/revoke
- **权限**：ADMIN
- **请求参数**：
```json
{
  "data": {
    "punishmentId": 1,
    "revokeReason": "表现良好，提前撤销"
  }
}
```

#### 7.10.5 查询处理结果列表
- **URL**：POST /api/punishment/resultList
- **权限**：关联处分记录可见
- **请求参数**：
```json
{
  "data": {
    "punishmentId": 1
  }
}
```

### 7.11 创新活动管理接口

#### 7.11.1 创建创新项目
- **URL**：POST /api/innovation/createProject
- **权限**：ADMIN, STUDENT
- **请求参数**：
```json
{
  "data": {
    "projectId": null, // 新增时为null
    "name": "人工智能研究",
    "category": "科研",
    "leaderId": 1,
    "teamMembers": "张三,李四",
    "description": "关于AI的研究项目",
    "status": 0 // 0-进行中，1-已完成，2-已终止
  }
}
```

#### 7.11.2 查询项目列表
- **URL**：POST /api/innovation/projectList
- **权限**：ADMIN, STUDENT, TEACHER
- **请求参数**：
```json
{
  "data": {
    "status": 0 // 状态筛选，可选
  }
}
```

#### 7.11.3 提交实践成果
- **URL**：POST /api/innovation/submitAchievement
- **权限**：ADMIN, STUDENT
- **请求参数**：
```json
{
  "data": {
    "projectId": 1,
    "personId": 1,
    "title": "研究成果论文",
    "content": "关于AI算法的创新点",
    "achievementType": "论文",
    "submitTime": "2026-06-06T10:00:00"
  }
}
```

#### 7.11.4 查询成果列表
- **URL**：POST /api/innovation/achievementList
- **权限**：ADMIN, STUDENT, TEACHER
- **请求参数**：
```json
{
  "data": {
    "projectId": 1, // 项目ID，可选
    "personId": 1   // 学生ID，可选
  }
}
```

### 7.12 活动管理接口

#### 7.12.1 发布校园活动
- **URL**：POST /api/activity/publish
- **权限**：ADMIN
- **请求参数**：
```json
{
  "data": {
    "name": "春季运动会",
    "location": "学校操场",
    "startTime": "2026-05-20T08:00:00",
    "endTime": "2026-05-21T18:00:00",
    "description": "年度春季运动会",
    "publisherId": 1
  }
}
```

#### 7.12.2 查询活动列表
- **URL**：POST /api/activity/list
- **权限**：ADMIN, STUDENT, TEACHER
- **请求参数**：无

#### 7.12.3 参与活动
- **URL**：POST /api/activity/join
- **权限**：ADMIN, STUDENT, TEACHER
- **请求参数**：
```json
{
  "data": {
    "activityId": 1,
    "personId": 1
  }
}
```

#### 7.12.4 查询参与记录
- **URL**：POST /api/activity/participation
- **权限**：ADMIN, STUDENT, TEACHER
- **请求参数**：
```json
{
  "data": {
    "activityId": 1, // 活动ID，可选
    "personId": 1    // 人员ID，可选
  }
}
```

### 7.13 统计分析接口

#### 7.13.1 获取主页数据
- **URL**：POST /api/statistics/getMainPageData
- **权限**：ADMIN, STUDENT, TEACHER
- **响应示例**：
```json
{
  "success": true,
  "code": 200,
  "message": "获取数据成功",
  "data": {
    "studentCount": 1500,
    "courseCount": 50,
    "attendanceRate": 95.5,
    "leaveApprovalRate": 87.2
  }
}
```

## 8. 系统实现

### 8.1 开发环境
- **操作系统**：Windows 10/11
- **开发工具**：IntelliJ IDEA
- **JDK版本**：Java 21
- **数据库**：MySQL 8.0
- **缓存**：Redis
- **构建工具**：Maven

### 8.2 项目结构

```
src/
├── main/
│   ├── java/
│   │   └── cn/
│   │       └── edu/
│   │           └── sdu/
│   │               └── java/
│   │                   └── server/
│   │                       ├── configs/          # 配置类
│   │                       ├── controllers/      # 控制器层
│   │                       ├── models/           # 实体类
│   │                       ├── payload/          # 数据传输对象
│   │                       ├── repositorys/      # 数据访问层
│   │                       ├── services/         # 业务逻辑层
│   │                       ├── uml/              # UML图
│   │                       ├── util/             # 工具类
│   │                       ├── JavaServerApplication.java
│   │                       └── SystemApplicationListener.java
│   └── resources/
│       ├── application.properties              # 应用配置
│       ├── static/                            # 静态资源
│       └── templates/                         # 模板文件
└── test/                                     # 测试代码
```

### 8.3 核心实现细节

#### 8.3.1 安全配置
使用Spring Security实现身份验证和授权：
- JWT Token认证机制
- 密码BCrypt加密存储
- 基于角色的访问控制
- 跨站请求伪造(CSRF)防护

#### 8.3.2 数据访问层
- 使用Spring Data JPA进行数据持久化
- 通过Repository接口实现CRUD操作
- 配置连接池优化数据库性能
- 使用事务管理保证数据一致性

#### 8.3.3 业务逻辑层
- 分离业务逻辑和数据访问逻辑
- 统一异常处理机制
- 参数验证和数据校验
- 业务规则实现

#### 8.3.4 控制器层
- RESTful API设计
- 统一请求响应格式
- 权限验证注解
- 数据绑定和转换

### 8.4 关键功能实现

#### 8.4.1 请假审批流程实现
请假审批功能通过三个状态字段(state)实现：
- 0：待审批
- 1：通过
- 2：驳回

审批过程支持教师和管理员两级审批，记录审批时间和意见。

#### 8.4.2 数据统计功能实现
系统提供多种统计查询：
- 班级学生数统计
- 课程平均分统计
- 课程考勤率统计
- 请假通过率统计

通过数据库视图和聚合函数实现高效统计。

#### 8.4.3 文件上传下载功能
- Excel文件导入导出功能
- 使用Apache POI处理Office文档
- 文件类型和大小限制
- 上传进度反馈

### 8.5 测试验证
- 单元测试：使用JUnit 5进行单元测试
- 集成测试：验证各模块间的协作
- 性能测试：验证系统在高并发下的表现
- 安全测试：验证身份认证和权限控制的有效性

### 8.6 部署方案
- 使用Maven构建可执行jar包
- 支持Docker容器化部署
- 配置外部MySQL和Redis服务
- 提供系统监控和日志管理

## 9. 总结

本学生管理系统实现了学生管理的核心功能，包括用户权限管理、学生信息管理、教学管理、考勤管理、请假审批、奖惩管理、创新活动管理等。系统采用现代化的技术架构，具有良好的安全性、可靠性和可扩展性，满足教育机构对学生管理的实际需求。

系统的设计充分考虑了用户体验和系统性能，在功能完整性的基础上，注重易用性和可维护性。通过标准化的API接口设计，系统具备良好的扩展能力，可以方便地与其他教育信息系统集成。
